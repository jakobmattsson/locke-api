{
  "name": "locke-api",
  "description": "A generic API for a locke-implementation, dealing with access control and encryption",
  "version": "0.3.2",
  "main": "./lib/index.js",
  "author": "Jakob Mattsson <jakob.mattsson@gmail.com> (jakobmattsson.se)",
  "engines": {
    "node": ">= 0.6.x < 0.10"
  },
  "dependencies": {
    "introspect": ">= 0.0.2 < 0.1",
    "bcrypt": ">= 0.7.3 < 0.8"
  },
  "devDependencies": {
    "underscore": ">= 1.4.4 < 2",
    "sinon": ">= 1.5.2 < 2",
    "nameify": ">= 0.1.1 < 0.2",
    "locke-store-mem": ">= 0.3.0 < 0.4",
    "locke-store-test": ">= 0.3.0 < 0.4",
    "jscov": ">= 0.5.4 < 0.6",
    "mocha-term-cov-reporter": ">= 0.1.0 < 0.2",
    "should": ">= 1.2.1 < 2",
    "mocha": ">= 1.8.1 < 2",
    "cucumber": ">= 0.3.0 < 0.4",
    "coffee-script": ">= 1.4.0 < 2"
  },
  "scripts": {
    "pretest": "coffee -co lib src",
    "prepublish": "coffee -co lib src",
    "coverage": "jscov src .cov && JSCOV=.cov mocha --compilers coffee:coffee-script --reporter mocha-term-cov-reporter tests/mocha/*.coffee",
    "test": "mocha --compilers coffee:coffee-script tests/mocha/*.coffee && cucumber-js tests/cucumber-features --require tests/cucumber-steps/api.coffee"
  }
}
